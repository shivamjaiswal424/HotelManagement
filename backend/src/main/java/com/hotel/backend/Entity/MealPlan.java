package com.hotel.backend.Entity;

/** EP = European (room only), CP = Continental (+breakfast), MAP = Modified American (+2 meals), AP = American (all meals) */
public enum MealPlan { EP, CP, MAP, AP }
